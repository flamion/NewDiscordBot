apply plugin: 'java'

jar {
    manifest {
        attributes(
                'Main-Class': 'discordMusicBot'
        )
    }
}
version '1.0-SNAPSHOT'

sourceCompatibility = 1.8

repositories {
    maven { url 'https://oss.sonatype.org/content/repositories/snapshots' }
    mavenCentral()
    jcenter()
}

dependencies {
    implementation 'com.discord4j:discord4j-core:3.1.0'
    implementation ("com.sedmelluq:lavaplayer:1.3.50") { changing = true }
}
